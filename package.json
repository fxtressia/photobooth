{
  "name": "photobooth-backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "type-gen": "wrangler types --env-interface CloudflareBindings",
      "type-check": "pnpm run type-gen && tsc --noEmit"
  },
  "dependencies": {
    "@auth0/auth0-hono": "^2.0.0-beta.0",
    "hono": "^4.12.25",
    "ulidx": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "wrangler": "^4.4.0"
  }
}